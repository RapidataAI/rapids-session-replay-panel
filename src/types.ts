export interface SessionReplayOptions {
  previewBaseUrl: string;
  canvasWidth: number;
  canvasHeight: number;
  maxIdleMs: number;
  cursorColor: string;
  rewardModal: boolean;
  debug: boolean;
  interact: boolean;
}
